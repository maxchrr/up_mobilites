#include <stdio.h>
#include "list.h"
#include "sort.h"
#include "utils.h"

int main(void)
{
	List_Bus_Line l;
	init_list(&l);
	/*l = insert_at_tail(l, 2); print_list(l);
	l = insert_at_tail(l, 4); print_list(l);
	l = insert_at_head(l, 6); print_list(l);
	l = insert_at_tail(l, 8); print_list(l);
	l = insert(l, 2, 99); print_list(l);
	l = delete_at_tail(l); print_list(l);
	l = delete_at_tail(l); print_list(l);
	l = delete_at_head(l); print_list(l);
	l = delete(l, 2); print_list(l);
	int s = 10;
	l = create_random(s); print_list(l);
	selection_sort(l); print_list(l);*/
	return 0;
}
